function saveCustomer(){const name=document.getElementById('name').value;const phone=document.getElementById('phone').value;const shop=document.getElementById('shop').value;const memo=document.getElementById('memo').value;if(name==='')return;const customers=JSON.parse(localStorage.getItem('customers')||'[]');customers.push({name,phone,shop,memo});localStorage.setItem('customers',JSON.stringify(customers));document.getElementById('name').value='';document.getElementById('phone').value='';document.getElementById('shop').value='';document.getElementById('memo').value='';loadCustomers();}function loadCustomers(){const list=document.getElementById('customerList');list.innerHTML='';const search=document.getElementById('searchInput').value.toLowerCase();let customers=JSON.parse(localStorage.getItem('customers')||'[]');customers.sort((a,b)=>a.name.localeCompare(b.name,'ko'));customers.forEach(c=>{const txt=`${c.name} ${c.shop} ${c.phone}`.toLowerCase();if(!txt.includes(search))return;const div=document.createElement('div');div.className='customer';div.innerHTML=`<div><strong>${c.name}</strong><p>${c.shop}</p><small>${c.phone}</small></div>`;list.appendChild(div);});}document.getElementById('searchInput').addEventListener('input',loadCustomers);loadCustomers();