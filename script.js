const calendarDays=document.getElementById('calendarDays');const monthTitle=document.getElementById('monthTitle');const date=new Date();monthTitle.innerText=`${date.getFullYear()}년 ${date.getMonth()+1}월`;const firstDay=new Date(date.getFullYear(),date.getMonth(),1).getDay();const lastDate=new Date(date.getFullYear(),date.getMonth()+1,0).getDate();for(let i=0;i<firstDay;i++){calendarDays.innerHTML+='<div></div>';}for(let d=1;d<=lastDate;d++){const div=document.createElement('div');div.className='day';if(d===date.getDate())div.classList.add('today');div.innerHTML=d;calendarDays.appendChild(div);}function addTodo(){const input=document.getElementById('todoInput');if(input.value==='')return;const todos=JSON.parse(localStorage.getItem('todos')||'[]');todos.push(input.value);localStorage.setItem('todos',JSON.stringify(todos));input.value='';loadTodos();}function loadTodos(){const list=document.getElementById('todoList');list.innerHTML='';const todos=JSON.parse(localStorage.getItem('todos')||'[]');todos.forEach(t=>{const div=document.createElement('div');div.className='todo blue';div.innerText=t;list.appendChild(div);});}const visits=['청담헤어','에이블샵','준오헤어','제이살롱','박원장'];function loadVisits(){const list=document.getElementById('visitList');visits.forEach(v=>{const div=document.createElement('div');div.className='customer';div.innerHTML=`<div><strong>${v}</strong><p>방문 예정 고객</p></div><input type='checkbox'>`;list.appendChild(div);});}loadTodos();loadVisits();